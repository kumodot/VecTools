Drop .hdr or .exr equirectangular environment maps in this folder.
VecTools lists them in the 3D tab (Environment > Rescan folder).

index.json lists the files for static hosts (GitHub Pages has no directory
listing). Locally the app also scans the folder, so you only need to edit
index.json when publishing. Keep maps around 1k-2k: plenty for lighting.
